package com.vasev.trainingapp.core.database.entity.types

/**
 * Prerequisite type for a program (e.g. required one-rep-max or required reps) / Тип условия программы (например, разовый максимум или число повторений)
 */
enum class PrerequisiteType {
    BODYWEIGHT,
    ONE_REP_MAX,
    REPS,
}
